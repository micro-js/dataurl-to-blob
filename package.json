{
  "name": "@f/dataurl-to-blob",
  "description": "Turn a dataURL into a blob in a cross-browser way",
  "repository": "git://github.com/micro-js/dataurl-to-blob.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "hihat": "^2.6.4",
    "tap-dev-tool": "^1.3.0",
    "tape": "^4.2.2"
  }
}
